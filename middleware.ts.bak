import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

// NOTE: This file is intentionally disabled to avoid Next.js build conflict
// "middleware.ts" vs "proxy.ts" (both detected as middleware by Next).
// Use ./proxy.ts only.

/*
export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  if (!pathname.startsWith("/admin")) return NextResponse.next();

  if (pathname === "/admin/login") return NextResponse.next();

  const sessionCookie = request.cookies.get("admin_session")?.value;
  const adminPassword = process.env.ADMIN_PASSWORD;

  // Accept either the cookie marker used by /api/admin/login
  // or (if ADMIN_PASSWORD is configured) the older cookie-value check.
  const isAuthed =
    sessionCookie === "logged_in" ||
    (adminPassword ? sessionCookie === adminPassword : false);

  if (!isAuthed) {
    const url = new URL("/admin/login", request.url);
    url.searchParams.set("next", pathname);
    return NextResponse.redirect(url);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/admin/:path*"],
};
*/
